@model IEnumerable<PROYECTO_BICICLETAS.Models.Producto>

@{
  ViewData["Title"] = "Catalogo de Indumentaria";
}
<h1 style="text-align:center;">Catalogo de Indumentaria</h1>

<br>
<nav aria-label="breadcrumb">
  <ol class="nav">
    <li class="breadcrumb-item"><a href="/Home/Index">Inicio</a></li>
    <li class="breadcrumb-item active" aria-current="page">Catalogo</li>
  </ol>
</nav>
<br>

<body class="back">
  <div class="container">
    <div class="row">
      @foreach (var item in Model)
      {
        <div class="col-sm-4">
          <div class="card mb-3">
            <div class="card-body">
              <div class="card-img-actions" align="center">
                <a asp-action="Details3" asp-route-id="@item.Id"><img class="img-responsive" width="190" height="200"
                  src="/img/@item.Image" alt="@item.Image" /></a>
              </div>
            </div>
            <div class="card-body co fo">
              <a asp-action="Details3" asp-route-id="@item.Id">
                <h5 class="card-title g900">@Html.DisplayFor(modelItem => item.Name)</h5>
              </a>
              <p class="card-text"> Precio: S/.@Html.DisplayFor(modelItem => item.Price)</p>
              <p class="card-text"> Stock:@Html.DisplayFor(modelItem => item.Amout)</p>
              <p class="card-text"> Stock: @Html.DisplayFor(modelItem => item.Amout) unidades</p>
                 @if(User.IsInRole("Administradores" )){      
              <a class="btn btn-info" asp-action="Details3" asp-route-id="@item.Id"><strong>Detalles</strong></a>
               }else if(User.IsInRole("OperadoresVentas" )){
                <a class="btn btn-info" asp-action="Details3" asp-route-id="@item.Id"><strong>Detalles</strong></a>
              }else{           
              <a class="btn btn-primary " asp-action="Add" asp-route-id="@item.Id"><strong>Añadir</strong></a>           
              <a class="btn btn-info" asp-action="Details3" asp-route-id="@item.Id"><strong>Detalle</strong></a>
              }         
            </div>
          </div>
        </div>

      }

    </div>
</body>